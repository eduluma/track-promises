# TEMPLATE — copy to 01-secrets.yaml and fill in real values.
# Do NOT commit 01-secrets.yaml to version control.
apiVersion: v1
kind: Secret
metadata:
  name: track-promises-secrets
  namespace: track-promises-prod
type: Opaque
stringData:
  POSTGRES_PASSWORD: CHANGE_ME
  DATABASE_URL: postgres://postgres:CHANGE_ME@track-promises-postgres:5432/track_promises
  NEXTAUTH_SECRET: CHANGE_ME
  # Optional: only needed if using a private GHCR image
  # GHCR_TOKEN: CHANGE_ME
